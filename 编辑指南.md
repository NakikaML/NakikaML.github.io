# 内容编辑指南

这份文件告诉你：**网站上的每一句话，分别在哪个文件里改。**

不用懂编程也能改。网站的文字都放在文件里，长这样：

```html
<h1>你好，我是 Nakika。</h1>
   ↑                    ↑
 标签                   你只改这里面的中文
```

**规则只有一条：只改 `>` 和 `<` 之间的中文，别动尖括号、别动 `{ }` 花括号里的东西。**

---

## 一、改完怎么立刻看到效果

打开终端（在项目目录下），运行：

```bash
pnpm.cmd dev
```

然后浏览器打开 **http://localhost:4321**

**改文件 → 保存 → 浏览器自动刷新。** 这是最常用的工作方式。

改满意了，运行一次检查（会告诉你有没有写坏）：

```bash
pnpm.cmd release
```

> Windows 下如果 `pnpm` 报「禁止运行脚本」，就用 `pnpm.cmd`。

---

## 二、最常改的 5 个地方（先看这张表）

| 网站上的什么 | 打开这个文件 |
| :--- | :--- |
| **站点名字、底栏、导航菜单、社交链接** | `src/site.config.ts` |
| **首页的大标题和自我介绍** | `src/pages/index.astro` |
| **「关于我」整页** | `src/pages/about.astro` |
| **「项目」页的介绍 + 项目清单** | `src/pages/projects.astro` 和 `src/data/projects.ts` |
| **笔记 / 博客 / 作品（内容本身）** | `src/content/notes/`、`src/content/blog/`、`src/content/works/` |

---

## 三、全站文案地图

### 1. 站点全局 —— `src/site.config.ts`

这一个文件管全站共用的信息。

| 改什么 | 对应字段 |
| :--- | :--- |
| 顶栏和页脚显示的名字 | `name: 'Nakika'` |
| 顶栏 Logo 里那个字母/字 | `mark: 'N'` |
| 搜索引擎看到的站点简介 | `description:` |
| 页脚版权署名 | `author: 'Nakika'` |
| **B站主页链接**（现在空的，要填） | `links: { bilibili: '' }` |
| 邮箱（现在空的，要填） | `links: { email: '' }` |
| GitHub 链接 | `links: { github: '...' }` |
| 顶栏导航菜单的文字和顺序 | `nav: [ { href, label } ]` |

**例子**——把 B站链接填上：

```ts
links: {
  email: 'nakika0511@outlook.com',
  bilibili: 'https://space.bilibili.com/你的UID',
  github: 'https://github.com/NakikaML',
  rss: '/rss.xml',
},
```

填了之后，页脚和「关于我」页会自动多出这些链接。留空字符串 `''` 就不显示。

**不想显示导航里的某一项**（比如笔记还没内容时），把那一行删掉或前面加 `//` 注释掉：

```ts
nav: [
  { href: '/', label: '首页' },
  { href: '/about/', label: '关于' },
  // { href: '/notes/', label: '笔记' },   ← 前面加 // 就不显示了
  { href: '/blog/', label: '博客' },
  { href: '/works/', label: '作品' },
  { href: '/projects/', label: '项目' },
],
```

---

### 2. 首页 —— `src/pages/index.astro`

| 网站上的哪句话 | 长什么样 |
| :--- | :--- |
| 小标题「网络工程在读 · 温州大学」 | `<p class="hero__eyebrow">网络工程在读 · 温州大学</p>` |
| 大标题「你好，我是 Nakika。」 | `<h1>你好，我是 Nakika。</h1>` |
| 那一段自我介绍 | `<p class="hero__lede"> ... </p>` |
| 三个按钮的文字 | `<a class="btn ...">看我的项目</a>` |
| 板块标题「在做的事」「博客」「精选作品」等 | `<h2>在做的事</h2>` |
| 笔记为空时的说明文字 | 搜「笔记板块正在重构中」 |

**注意**：首页的板块是**自动隐藏**的——没有项目就不显示项目板块，没有作品就不显示作品板块。
所以你不用管「怎么删掉空板块」，它自己会判断。

「精选作品」最多显示 3 件：标了 `featured: true` 的排前面，不足 3 件时自动用最新作品补齐
（一件都没标就是最新 3 件）。所以**不用凑数**，标 1 件也不会让首页空半屏。

---

### 3. 关于我 —— `src/pages/about.astro`

整页都是手写文案，从 `<h1>关于我</h1>` 往下都是。

页面用 `<h2>` 分小节：我是谁 / 在学什么 / 在做什么 / 这个网站有什么 / 价值观 / 联系我。
每一节的标题和正文都可以直接改。

里面的 `<table>` 是那张「板块 → 内容」的表格，`<ul><li>` 是项目符号列表。

> 💡 这一段现在的内容是我根据你的个人简介整理的（没有放成绩、规划那些隐私信息）。
> 你自己读一遍，措辞按你的习惯改。

---

### 4. 项目页 —— `src/pages/projects.astro` + `src/data/projects.ts`

**页头文案**（标题、「课程之外真正动手做的东西」那句）在 `src/pages/projects.astro`。

**项目清单**在 `src/data/projects.ts`，格式是这样，照着加就行：

```ts
export const projects: Project[] = [
  {
    name: '外审专家管理系统',              // 项目名
    tagline: '一句话定位',                  // 副标题
    description: '详细介绍，可以写几句。',   // 正文
    role: '开发成员',                       // 你的角色
    period: '2026.06 – 至今',               // 时间
    status: '进行中',                       // 进行中 / 已完成 / 筹备中
    stack: ['数据库系统', 'Java'],           // 技术栈标签
    link: '',                               // 外部链接，没有就留空
  },
  // 加新项目就照着复制一段，注意每一项之间用逗号隔开
];
```

---

### 5. 笔记列表页 —— `src/pages/notes/index.astro`

页头三行：`Notes`（小标签）、`学习笔记`（大标题）、下面那段说明。

还有「目前还没有内容。这是有意为之，不是出错了。」那段——措辞可以按你的想法改。

---

### 6. 知识地图页 —— `src/pages/maps/index.astro`

页头文案 + 空状态说明。

---

### 7. 博客列表页 —— `src/pages/blog/index.astro`

页头文案 + 空状态说明。

---

### 8. 作品集页 —— `src/pages/works/index.astro`

页头文案 + 空状态说明。

---

### 9. 404 页 —— `src/pages/404.astro`

「这个页面不存在」「可能是链接过期了，或者这篇笔记还没公开。」两句话都可以改。

---

### 10. 浏览器标签页图标 —— `public/favicon.svg`

现在是一个紫色圆角方块加一个 `M` 字母的图形。
想换成自己的，用任意 SVG 编辑器改，或直接把文件替换成你自己的 `.svg`。

---

### 11. 搜索引擎配置 —— `robots.txt` 和 `astro.config.mjs`

- `public/robots.txt` —— 控制爬虫，一般不用动
- `astro.config.mjs` 里的 `SITE = 'https://nakikaml.github.io'` —— **换成自定义域名时改这里**

---

## 四、怎么加内容

### 📝 一篇笔记

在 `src/content/notes/` 新建 `.md` 文件。**最省事的做法：复制 `_模板-网站笔记.md`，改个名再改内容。**

文件名就是网址。建议用英文小写 + 连字符，例如 `jvm-memory-model.md` → 网址 `/notes/jvm-memory-model/`。

文件开头那段被 `---` 包起来的是「属性」，必须填 `title`，其余可选：

```markdown
---
title: JVM 内存模型到底在讲什么
description: 一句话摘要，会显示在卡片上
category: 计算机科学
date: "2026-09-22"
tags: [Java, JVM]
draft: true          # ← 改成 false 才发布
---

正文从这里开始写，正常用 Markdown 就行。
```

### ✍️ 一篇文章（博客）

在 `src/content/blog/` 新建 `.md`。**复制 `_模板-博客.md`** 改名即可。

字段：`title` / `description` / `date` / `tags` / `draft`。

### 🎙️ 一个作品（配音 / 翻唱）

在 `src/content/works/` 新建 `.md`。那里已经有三个模板：
`_模板-配音.md`、`_模板-翻唱.md`、`_模板-知识分享.md`。

封面图放到 `public/works/` 目录下，然后 `cover` 字段填 `/works/你的图.jpg`。

### 🖼️ 在文章里插图

图片放到 `public/notes-assets/`，正文里写：

```markdown
![图片说明](/notes-assets/你的图.png)
```

---

## 五、会写 Markdown 就够了

正文里常用的写法：

| 想要的效果 | 怎么写 |
| :--- | :--- |
| 二级标题 | `## 标题` |
| 三级标题 | `### 标题` |
| **加粗** | `**文字**` |
| *斜体* | `*文字*` |
| 链接 | `[显示的文字](https://网址)` |
| 列表 | 行首写 `- ` |
| 引用 | 行首写 `> ` |
| 行内代码 | 用反引号包起来 |
| 代码块 | 上下各三个反引号，开头可以注明语言如 ` ```cpp ` |
| 表格 | 见模板文件里的例子 |

**公式**：

- 行内公式 `$a^2 + b^2 = c^2$`
- 块级公式上下各写一行 `$$`

> ⚠️ **多行公式必须把 `$$` 单独放一行**，不要写成 `$$\begin{aligned} ...`。
> 这是本项目实际踩过的坑——那样写 `\begin{...}` 会被吞掉，页面上会显示成一坨红色错误文字。

---

## 六、三个最容易踩的坑

### 1. 日期别忘了加引号

```markdown
date: 2026-09-22      ❌ 会被当成日期对象，构建报错
date: "2026-09-22"    ✅ 加引号
```

（我已经让代码尽量兼容两种写法了，但加引号最保险。）

### 2. 别动花括号

看到 `<h1>{ 什么东西 }</h1>` 这种，`{ }` 里面是程序在算的东西，改了会报错。
只改花括号**外面**的中文。

### 3. `draft: true` 记得改掉

新建的文件默认是 `draft: true`（草稿，不发布）。
写完了要把 `true` 改成 `false`，不然网站上看不到，容易以为自己写错了。

---

## 七、改坏了怎么办

**先别慌，有后悔药。**

- `pnpm dev` 的终端里如果出现红色报错，它会告诉你是哪个文件第几行出的问题
- 浏览器上如果出现一片错误遮罩，也是同样信息
- 实在改坏了：告诉我，或者用 `git` 撤销

```bash
git diff                      # 看这次改了哪些文件
git checkout -- 某个文件       # 撤销对某个文件的修改
```

---

## 八、当前有哪些是「占位内容」等着你改

| 位置 | 现状 |
| :--- | :--- |
| `src/site.config.ts` | B站链接、邮箱是**空的**，需要你填 |
| `src/pages/about.astro` | 我根据你的个人简介整理了一版，需要你按自己的口吻改 |
| `src/pages/index.astro` | 首页自我介绍是通用版，建议改成你自己的说法 |
| `src/data/projects.ts` | 四条项目记录，需要核对是否准确 |
| `src/content/blog/` | **空的**，只有模板，等你自己写 |
| `src/content/notes/` | **空的**，只有模板 |
| `src/content/works/` | **空的**，只有三个模板 |
| `public/favicon.svg` | 默认图标，可以换 |

改完之后跑一次：

```bash
pnpm.cmd release
```

看到 `✅ 全部检查通过` 就说明没问题。
