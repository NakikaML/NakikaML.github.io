---
title: 把这里换成你的笔记标题
description: 一句话摘要，会显示在列表页和搜索结果里。建议 60 字以内。
category: 计算机科学
date: "2026-09-22"
tags: [示例标签]
draft: true
---

<!--
  ══════════════════════════════════════════════════════════════
  这是「网站笔记」的模板。网站上的笔记全部是手写重构的，
  不从 Obsidian 直接搬 —— 因为私人笔记有跳步、缩写和只有自己
  看得懂的上下文，直接发布对读者没有价值。

  用法：
  1. 复制这个文件，改成你想要的文件名
     （文件名就是 URL，用英文小写 + 连字符最稳，例如 jvm-memory-model.md）
  2. 改上面的 frontmatter
  3. 把 draft 改成 false 才会发布
  4. 正文就正常写 Markdown

  frontmatter 字段说明（都可以留空，只有 title 必填）：
    title       标题（必填）
    description 摘要，显示在卡片上；不填会从正文自动截取
    category    分类，会出现在筛选按钮里，例如「计算机科学」「语言学习」
    kind        note = 普通笔记（默认）；moc = 知识地图（总览索引页）
    date        日期，YYYY-MM-DD
    tags        标签数组
    difficulty  难度：基础 / 中等 / 困难
    draft       true = 草稿不发布
    featured    true = 在首页精选展示

  正文支持：Markdown 全语法、表格、代码高亮、LaTeX 公式、Mermaid 图。

  ⚠️ 写公式的一个提醒：多行公式请把 $$ 单独放一行，例如

        $$
        \begin{aligned}
        a &= b \\
        c &= d
        \end{aligned}
        $$

     不要写成 $$\begin{aligned} ...，那样 remark-math 会吞掉 \begin{...}，
     KaTeX 会报错并在页面上显示成红色错误文字。
  ══════════════════════════════════════════════════════════════
-->

## 第一节

正文写在这里。

## 第二节

需要的话可以插表格：

| 项目 | 说明 |
| :--- | :--- |
| A | 这是 A |

代码块：

```cpp
#include <iostream>
int main() {
    std::cout << "hello" << std::endl;
}
```

公式（行内 $a^2 + b^2 = c^2$，块级见上面的提醒）。
